# Local web app (wizard + viewer) server deps. Installed into the shared
# worker/.venv. The heavy ML stack stays in worker/requirements-ml.txt.
fastapi>=0.110
uvicorn>=0.29
httpx>=0.28        # server needs it at import (conn_check Test-connections)
